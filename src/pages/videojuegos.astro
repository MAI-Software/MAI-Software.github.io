---
import BaseLayout from '../layouts/BaseLayout.astro';
import ProjectArchive from '../components/projects/ProjectArchive.astro';
import { company } from '../data/company';
import { getCategory } from '../data/categories';
import { getProjects } from '../lib/projects';

const locale = 'es';
const category = getCategory('game');
const projects = await getProjects(locale, 'game');
---

<BaseLayout
  locale={locale}
  title={`${category.label.es} — ${company.name}`}
  description={category.intro.es}
  alternatePath="/en/games"
>
  <ProjectArchive
    locale={locale}
    title={category.label.es}
    intro={category.intro.es}
    projects={projects}
    activeCategory="game"
  />
</BaseLayout>
